import { Box, Typography } from '@mui/material';

const NotTranscribable = () => {
  return (
    <Box pt={4}>
      <Typography variant="h3">Video cannot be transcribed</Typography>
    </Box>
  );
};

export default NotTranscribable;
